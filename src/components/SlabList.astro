<ol class='slablist'>
  <slot />
</ol>

<style>
  ol.slablist {
    /* don't actually display as list */
    list-style: none;

    /* make it flex rows with very wide children, 1 per row */
    display: flex;
    flex-direction: column;
    align-items: center;

    /* no extra space, just between children */
    margin: 0;
    padding: 0;
    /* gap: 0.5em; */
  }
</style>
